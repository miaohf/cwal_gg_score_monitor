# 用户使用说明

## 启动前准备

请确认程序目录下包含：

- 程序文件
- `README.md`
- `users.csv`
- `.env`（如使用 `.env` 配置 API）

`users.csv` 用于配置选手列表，格式为：

```csv
name,cwal_gg_id
Fengzi,scgotoboy
```

## 基本操作

### Windows 版本

- 拖动顶部区域：移动窗口
- 拖动窗口边缘：调整窗口大小
- 右键窗口或按 `F2`：打开设置
- 双击选手行：手动修改该选手积分
- 右上角 `X`：关闭程序
- `Esc`：关闭设置窗口或手动积分窗口
- `+` / `↑`：降低透明度
- `-` / `↓`：提高透明度



## 设置项说明

`Font Size`：榜单字体大小。

`Font Color`：榜单文字颜色。

`Font Type`：榜单字体样式，例如 `Regular`、`Bold`、`Monospace`。

`BG %`：背景透明度。Windows 下影响透明覆盖窗口；Linux 下影响应用内部背景。

`Poll(s)`：轮询设置项。目前实际更新节奏按下一分钟整点执行，例如 `14:54:06` 启动会在 `14:55:00` 更新。

`Hold(s)`：手动分数保留时间。双击选手手动输入分数后，在这段时间内不会被 API 自动覆盖。

`Stop Time`：停止更新时间，格式为：

```text
YYYY-MM-DD HH:MM
```

例如：

```text
2026-05-18 23:45
```

清空 `Stop Time` 表示持续更新。达到 `Stop Time` 后停止拉取 API，并显示前三名金银铜牌。

## 榜单显示规则

- 每分钟整点自动更新一次。
- 成功更新的选手显示绿灯，并保持到下一轮。
- 下一轮失败或本轮未更新的选手不显示绿灯。
- 达到 `Stop Time` 后隐藏绿灯，前三名显示金银铜牌。
- 只有前 8 名有背景色，第 9 名及以后没有背景色。
- 积分颜色：
  - `2200+`：青蓝色
  - `2300+`：亮紫蓝色
  - `2400+`：金色

## 手动修改积分

双击某个选手行可以打开手动积分窗口。

- 输入新积分后点击 `Save` 保存。
- 在输入框中按 `Enter` 等同于点击 `Save`。
- 打开窗口时，光标会自动移动到当前积分末尾。
- 手动积分会在 `Hold(s)` 设置的时间内保留。
- 保留时间结束后，下一轮成功更新会用 API 分数覆盖手动分数。

## 日志查看

API 请求和轮询日志写在程序运行目录下：

```text
api_fetch.log
```

如果发现积分没有更新，可以查看该文件，确认最近是否有新的更新记录。
